[pytest]
# The service is a flat module rather than an installed package, so the repository root
# must be importable when pytest is invoked directly (as it is in CI).
pythonpath = .
testpaths = tests
addopts = -ra
